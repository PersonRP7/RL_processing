# Treat text files consistently
* text=auto

# Force LF for shell scripts
*.sh text eol=lf

# Force LF for config files and lockfiles
*.toml text eol=lf
*.lock text eol=lf
.env text eol=lf

# Force LF for .py files
*.py text eol=lf

# Do not change binary files
*.png -text
*.jpg -text
*.ico -text